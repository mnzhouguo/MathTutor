# 4. 动态实验工具系统

## 功能定位
为抽象的数学知识点创建动态、可交互的实验工具,帮助学生通过可视化操作理解数学概念。

## 4.1 实验工具概述

### 核心理念
通过动态可视化的实验工具,将抽象的数学概念转化为直观的图形和动画,降低理解难度。

### 实验工具生成流程

```mermaid
知识点分析
  ↓
AI判断需要可视化
  ↓
生成实验工具代码（离线）
  ↓
代码审核与测试
  ↓
工具入库
  ↓
教学时动态调用
```

## 4.2 核心实验工具类型

### 4.2.1 数轴动点可视化工具

**功能特性**
- 支持添加多个动点（2-4个）
- 实时显示点的坐标、路程、速度
- 支持相遇/追及时刻自动标注
- 可调节播放速度（0.5x - 2x）
- 支持暂停、继续、重置

**应用知识点**
- KP02_1: 路程与坐标互化
- KP02_2: 追及与相遇
- KP02_3: 中点公式
- KP02_4: 分类讨论

**界面原型**

```
┌─────────────────────────────────────────────────┐
│ 📐 数轴动点实验工具                             │
├─────────────────────────────────────────────────┤
│                                                  │
│  数轴                                           │
│  ─────────────────────────────────────          │
│  -10      A(-3)     0       B(6)         10     │
│              ●────────────────●                 │
│                                                  │
│  动点M: ●───────────────→                       │
│  速度: 2单位/秒 | 坐标: -3+2t = 1 | t=2s        │
│                                                  │
│  动点N: ●──────←                                │
│  速度: 3单位/秒 | 坐标: 6-3t = 0 | t=2s        │
│                                                  │
├─────────────────────────────────────────────────┤
│  ⏸ 暂停  ▶ 继续  🔄 重置                        │
│  播放速度: [━━○━] 1.0x                          │
│  显示: ☑坐标 ☑路程 ☑速度 ☑相遇点               │
└─────────────────────────────────────────────────┘
```

### 4.2.2 绝对值函数图像生成器

**功能特性**
- 支持单个或多个绝对值相加
- 自动寻找零点并分段
- 分段着色显示
- 鼠标悬停显示该段表达式
- 动态修改参数实时更新图像

**应用知识点**
- KP01_1: 零点分段法

**界面原型**

```
┌─────────────────────────────────────────────────┐
│ 📊 绝对值函数图像生成器                         │
├─────────────────────────────────────────────────┤
│                                                  │
│  输入表达式:                                    │
│  y = |x-1| + |x+2|                              │
│  [修改表达式]                                   │
│                                                  │
│  零点分析:                                      │
│  • x-1 = 0 → x = 1                              │
│  • x+2 = 0 → x = -2                             │
│  分段: (-∞,-2] | [-2,1] | [1,+∞)               │
│                                                  │
│  图像:                                          │
│       y                                          │
│       │         ╱│╲                              │
│    6  │        ╱ │ ╲                             │
│    4  │       ╱  │  ╲                            │
│    2  │   ───╱   │   ╲───                       │
│    0  │───┬────┼────┬────── x                   │
│       │  -2    │    1                            │
│  -2   │        │                                │
│                                                  │
│  鼠标悬停在图像上查看各段表达式                  │
└─────────────────────────────────────────────────┘
```

### 4.2.3 整体代入可视化工具

**功能特性**
- 高亮显示可替换的整体部分
- 动画演示代入过程
- 分步骤展示化简过程
- 支持用户输入自定义表达式

**应用知识点**
- KP03_1: 整体代入法
- KP03_2: 降幂策略

### 4.2.4 动态线段与中点工具

**功能特性**
- 拖拽点实时更新
- 自动标注中点位置
- 显示线段长度计算
- 支持几何关系标注

**应用知识点**
- KP02_3: 中点公式
- KP08_1: 双中点模型
- KP08_3: 方程思想

## 4.3 AI生成实验工具

### 生成提示词模板

```system
你是一位数学可视化工具开发专家。

【任务】
为给定的数学知识点生成一个交互式实验工具。

【知识点信息】
知识点ID: {kp_id}
知识点名称: {kp_name}
知识点说明: {kp_detail}

【工具要求】
1. 使用HTML5 Canvas或SVG进行渲染
2. 提供交互控件（滑块、按钮、输入框）
3. 动画展示数学概念的变化过程
4. 实时显示相关数值
5. 界面简洁，符合教学场景

【输出格式】
包含以下代码的单一HTML文件：
- HTML结构
- CSS样式（响应式设计）
- JavaScript交互逻辑

【参考示例】
{similar_tool_example}

请生成工具代码：
```

### 工具元数据结构

```json
{
  "tool_id": "T_KP02_2_NUMBERLINE",
  "tool_name": "数轴动点可视化工具",
  "target_kp": ["KP02_1", "KP02_2", "KP02_3", "KP02_4"],
  "tool_type": "interactive_simulation",

  "description": "动态展示数轴上多个动点的运动过程，
    帮助学生理解追及相遇问题的路程关系",

  "features": [
    "支持添加2-4个动点",
    "实时显示坐标、路程、速度",
    "自动标注相遇/追及时刻",
    "可调节播放速度"
  ],

  "technical_stack": {
    "render": "HTML5 Canvas",
    "interaction": "Vanilla JavaScript",
    "animation": "requestAnimationFrame"
  },

  "code_files": {
    "html": "templates/tools/numberline.html",
    "css": "assets/css/tools/numberline.css",
    "js": "assets/js/tools/numberline.js"
  },

  "version": "1.2.0",
  "created_at": "2024-01-15",
  "updated_at": "2024-01-20",
  "tested_browsers": ["Chrome 120+", "Firefox 120+", "Safari 17+"]
}
```

## 4.4 实验工具在教学中的应用

### 调用时机

**触发条件**
1. 学生在同一知识点上连续答错3次
2. 学生主动点击"打开实验工具"
3. AI教师判断需要可视化辅助
4. 题目逻辑链中标注了"使用工具"

### 工具调用接口

```javascript
// 在教学界面中调用实验工具
function invokeTool(toolId, context) {
  return {
    toolId: toolId,
    parameters: {
      // 从题目中提取的参数
      points: [
        {name: "A", position: -3, velocity: 2},
        {name: "B", position: 6, velocity: -3}
      ],
      animation: {
        duration: 10000,  // 10秒
        speed: 1.0
      }
    },
    onToolClose: (result) => {
      // 工具关闭后的回调
      // 学生是否理解？是否需要再次演示？
    }
  }
}
```

### 教学引导流程

```
┌─────────────────────────────────────────────────┐
│ 👩‍🏫 AI老师:                                     │
│ "这道题涉及动点相遇问题，可能有点抽象。        │
│  让我们用数轴工具来直观地看一下。"              │
│                                                  │
│  [📐 打开数轴实验工具]                          │
│                                                  │
│ ─────────────────────────────────────────────  │
│ (工具打开，展示动画)                            │
│                                                  │
│ 👩‍🏫 AI老师:                                     │
│ "看到没有？M和N在2秒时相遇了。                  │
│  相遇点在哪里？对，在坐标1的位置。"             │
│                                                  │
│ "现在你明白为什么路程之和等于AB的总距离了吗？"  │
│  [我明白了] [再放一遍] [回到题目]               │
└─────────────────────────────────────────────────┘
```

## 4.5 工具管理与更新

### 工具质量评估

**评估维度**
- **准确性**：数学概念展示是否准确
- **交互性**：用户操作是否流畅自然
- **教学性**：是否能帮助理解概念
- **性能**：动画是否流畅（60fps）
- **兼容性**：是否支持主流浏览器

### 版本迭代

**更新触发**
- 用户反馈工具存在问题
- 知识点更新导致工具需要调整
- 定期优化（每学期）

**版本管理**

```json
{
  "tool_version": {
    "tool_id": "T_KP02_2_NUMBERLINE",
    "current_version": "1.2.0",
    "changelog": [
      {
        "version": "1.2.0",
        "date": "2024-01-20",
        "changes": [
          "新增异步启动模式",
          "优化动画流畅度",
          "支持更多动点（最多4个）"
        ],
        "author": "AI_Generated",
        "approved": true
      },
      {
        "version": "1.1.0",
        "date": "2024-01-10",
        "changes": ["支持移动端触摸操作"],
        "author": "human_dev"
      }
    ]
  }
}
```

## 4.6 学生使用数据收集

### 使用记录

```json
{
  "tool_usage": {
    "student_id": "S2024001",
    "tool_id": "T_KP02_2_NUMBERLINE",
    "usage_count": 5,
    "last_used": "2024-01-20T10:30:00Z",

    "sessions": [
      {
        "session_id": "US001",
        "timestamp": "2024-01-20T10:30:00Z",
        "duration": 180,  // 3分钟
        "context": {
          "problem_id": "P_MATH_2024_025",
          "step_number": 2
        },
        "interactions": {
          "play_count": 3,
          "pause_count": 2,
          "speed_adjustments": [0.5, 1.0, 1.5]
        },
        "understanding_after_use": true
      }
    ],

    "effectiveness": {
      "helpful_rate": 0.8,  // 80%的使用后理解了概念
      "avg_duration": 150,  // 平均使用2.5分钟
      "most_used_features": ["play_animation", "show_coordinates"]
    }
  }
}
```

### 效果评估

**评估指标**
- 工具使用后学生的答题正确率提升
- 工具使用后学生需要的提示次数减少
- 学生主动使用工具的频率
- 学生对工具的主观评价（1-5星）

## 4.7 工具扩展接口

### 自定义工具开发

支持教师或高级开发者创建自定义工具：

```javascript
// 自定义工具模板
class CustomMathTool {
  constructor(containerId, config) {
    this.container = document.getElementById(containerId);
    this.config = config;
    this.init();
  }

  init() {
    // 初始化工具界面
  }

  render() {
    // 渲染可视化内容
  }

  update(param) {
    // 根据参数更新显示
  }

  exportData() {
    // 导出工具产生的数据
    // 用于分析学生使用情况
  }

  destroy() {
    // 清理资源
  }
}
```

## 4.8 技术实现要点

- 渲染引擎：HTML5 Canvas / SVG
- 动画库：GSAP / Anime.js / 原生 requestAnimationFrame
- 响应式设计：CSS Grid + Flexbox
- 性能优化：Web Worker 处理复杂计算
- 跨浏览器兼容：Polyfill + 特性检测
- 移动端适配：触摸事件支持
- 无障碍设计：ARIA标签 + 键盘导航
